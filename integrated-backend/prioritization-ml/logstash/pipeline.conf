input {
  tcp {
    port => 5000
    codec => json_lines
  }
}

filter {
  # optional: add tags/fields, parse extra fields, etc.
}

output {
  stdout { codec => rubydebug }
  # or send to Elasticsearch:
  # elasticsearch { hosts => ["http://elasticsearch:9200"] index => "priority-logs-%{+YYYY.MM.dd}" }
}
